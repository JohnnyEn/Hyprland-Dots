local configDir = os.getenv("HOME") .. "/.config/hypr"
local scriptsDir = configDir .. "/scripts"
local UserConfigs = configDir .. "/UserConfigs"

--hl.bind(
--	"switch:off:Lid Switch",
	--hl.dsp.exec_cmd(echo "monitor = eDP-1, preferred, auto, 1" > $UserConfigs/LaptopDisplay.conf),
	--{ locked = true }
--)
--hl.bind("switch:on:Lid Switch", hl.dsp.exec_cmd(echo "monitor = eDP-1, disable" > $UserConfigs/LaptopDisplay.conf), { locked = true })
